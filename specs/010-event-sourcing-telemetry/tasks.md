# Tasks: Event Sourcing for Telemetry and Aggregates

**Input**: Design documents from `/specs/010-event-sourcing-telemetry/`

**Prerequisites**: plan.md (required), spec.md (required for user stories), research.md, data-model.md, quickstart.md

**Tests**: Playback integration tests will be appended to our centralized test runner to verify correct chronological sorting and calculation logic.

**Organization**: Tasks are grouped by user story to enable independent implementation and testing of each story.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2)
- Include exact file paths in descriptions

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Preparing baseline logging structures

- [X] T001 Initialize and verify a helper utility inside `backend/sim_server.py` to generate unique lexicographically sortable sort keys for events (`event#<iso_utc_timestamp>#<short_uuid_hash>`)

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Standardizing the event appending helper to decouple endpoints

**⚠️ CRITICAL**: No user story work can begin until this phase is complete

- [X] T002 Implement a standard, unified `append_telemetry_event(user_id, event_type, payload)` helper function inside `backend/sim_server.py` that utilizes `db_put_item` under the hood

**Checkpoint**: Foundation ready - user story implementation can now begin

---

## Phase 3: User Story 1 - Immutable Event Recording (Priority: P1) 🎯 MVP

**Goal**: Seamlessly record every mutative telemetry change as an immutable event alongside updating totals

**Independent Test**: Verify that sending a POST to `/beer` generates both a new unique event log and the updated snapshot totals inside the database.

### Implementation for User Story 1

- [X] T003 [US1] Refactor `/beer` POST handler inside `process_api_post` of `backend/sim_server.py` to call `append_telemetry_event` for each drink or reset event logged
- [X] T004 [US1] Refactor `/steps` POST handler inside `process_api_post` of `backend/sim_server.py` to append a `steps_logged` event log item on every steps update
- [X] T005 [US1] Refactor `/sensecap` and `/browan` POST handlers inside `process_api_post` of `backend/sim_server.py` to append environmental telemetries to the event store
- [X] T006 [US1] Refactor `/monzo-sync-simulation` POST handler inside `process_api_post` of `backend/sim_server.py` to append transaction event log records to the event store

**Checkpoint**: At this point, User Story 1 (the immutable append-only event store dual-write) is fully functional.

---

## Phase 4: User Story 2 - State Reconstruction / Playback (Priority: P2)

**Goal**: Dynamically reconstruct camper aggregate state from raw events up to a target timestamp

**Independent Test**: Request `GET /playback?user_id=cha&until=2026-07-13T12:00:00Z` and verify that the replayed state accurately aggregates and returns the count.

### Implementation for User Story 2

- [X] T007 [US2] Implement the `process_playback(user_id, until_timestamp)` logic in `backend/sim_server.py` to read all event logs up to `until_timestamp` and sequentially replay them in-memory
- [X] T008 [US2] Implement the `/playback` route matching in `process_api_get` of `backend/sim_server.py` and map it to return the reconstructed payload generated by `process_playback`

**Checkpoint**: All user stories should now be independently functional.

---

## Phase 5: Polish & Cross-Cutting Concerns

**Purpose**: Testing, verification, and code optimization

- [X] T009 Add comprehensive `/playback` route validation test cases into `backend/test_endpoints.py` confirming chronological ordering and correct time boundary truncation
- [X] T010 Run and complete the entire `specs/010-event-sourcing-telemetry/quickstart.md` validation scenarios against our local Python server

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: Can start immediately.
- **Foundational (Phase 2)**: Depends on Setup (Phase 1) - BLOCKS all user stories.
- **User Stories (Phase 3 & 4)**: All depend on Foundational (Phase 2) completion.
  - User Story 1 (P1) is the MVP and must be completed first to accumulate events.
  - User Story 2 (P2) depends on events generated by US1.
- **Polish (Final Phase)**: Depends on all desired user stories being complete.

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup
2. Complete Phase 2: Foundational (CRITICAL)
3. Complete Phase 3: User Story 1 (Immutable appends)
4. **STOP and VALIDATE**: Verify that events are logged correctly in the flat JSON file/database.

### Incremental Delivery

1. Complete Setup + Foundational → Foundation ready.
2. Add User Story 1 → Test independently (MVP!).
3. Add User Story 2 → Test state reconstruction independently.
4. Run Polish & validation checklist.
